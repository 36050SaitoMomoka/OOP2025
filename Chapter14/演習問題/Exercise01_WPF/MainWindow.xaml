<Window x:Class="Exercise01_WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ファイル読み込みアプリ" Height="450" Width="800">
    <DockPanel>
        <!-- ステータスバー -->
        <StatusBar DockPanel.Dock="Bottom">
            <TextBlock Name="StatusText" />
        </StatusBar>

        <!-- メインコンテンツ -->
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- ファイルパス表示 -->
            <TextBox Name="TextBoxFilePath" Grid.Row="0" Grid.Column="0" Margin="0,0,10,10" Height="25" />

            <!-- ファイル選択ボタン -->
            <Button Name="ButtonOpenFile" Grid.Row="0" Grid.Column="1" Content="ファイル選択" Margin="0,0,10,10" Width="100" Click="ButtonOpenFile_Click"/>

            <!-- 読み込みボタン -->
            <Button Name="ButtonReadFile" Grid.Row="0" Grid.Column="2" Content="読み込み" Margin="0,0,0,10" Width="100" Click="ButtonReadFile_Click"/>

            <!-- ファイル内容表示 -->
            <TextBox Name="TextBoxContent" Grid.Row="2" Grid.ColumnSpan="3" 
                     TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"
                     Margin="0,10,0,0"/>
        </Grid>
    </DockPanel>
</Window>